{% extends "base.html.twig" %}

{% block stylesheets %}
    {{ parent() }}
{% endblock %}

{% block main %}

<div class="row">
    <div class="col-xl-12 col-lg-12 col-md-12 col-sm-12 col-12 mb40 text-center mt-4">
        <h1>Tableau de bord</h1>
        {% if is_granted('ROLE_ADMIN') %}
            <a href="{{ path('logout') }}">Se déconnecter</a>
        {% endif %}
    </div>
</div>
<div class="content">
    <div class="container">
        <div class="row">
            <div class="col-xl-6 col-lg-6 col-md-6 col-sm-12 col-12 text-center">
                <a href="{{ path('admin_liste_message') }}" class="btn btn-primary mb-2"><i class="fas fa-users mr-2"></i>Voir la liste des messages</a>
            </div>

            <div class="col-xl-6 col-lg-6 col-md-6 col-sm-12 col-12 text-center">
                <a href="" class="btn btn-primary mb-2"><i class="fas fa-address-card mr-2"></i>Gérer les dates compétitions</a>
            </div>
        </div>
    </div>
</div>

{% endblock %}

{% block javascripts %}
    {{ parent() }}
{% endblock %}